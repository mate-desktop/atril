to run these test successfully you need to install:
python-dogtail 
python-pyatspi2

then issue the command `sniff`. 

If everything is OK, you can run these tests with `python test1.py`
for example. 


